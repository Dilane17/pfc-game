// Fonctions utilitaires génériques

// Fonction pour obtenir le choix de l'IA
function getAIChoice() {
    const randomIndex = Math.floor(Math.random() * choices.length);
    return choices[randomIndex];
}

